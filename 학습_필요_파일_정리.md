# 🚇 학습에 필요한 핵심 파일 정리

## 📁 필수 코드 파일

### 1. **모델 코드** (`models/`)
```
models/
├── dcrnn_layers.py      # ✅ DCRNN 모델 구현 (Diffusion Convolution, DCGRU, Attention)
├── dataset.py           # ✅ PyTorch Dataset 클래스 (SubwayGraphDataset)
├── losses.py            # ✅ 손실 함수 (MultiTaskLoss 등)
└── __init__.py          # ✅ 패키지 초기화
```

### 2. **학습 스크립트** (`scripts/`)
```
scripts/
└── train_occupancy_model.py  # ✅ 메인 학습 스크립트
```

### 3. **평가 스크립트** (`scripts/`)
```
scripts/
└── evaluate_occupancy_seasonal.py  # ✅ 모델 평가 스크립트
```

### 4. **설정 파일** (`config/`)
```
config/
└── model_config.yaml    # ✅ 하이퍼파라미터 설정
```

---

## 📊 필수 데이터 파일

### 1. **핵심 데이터** (`data/`)
```
data/
├── 혼잡도.csv              # ✅ 혼잡도 타깃 데이터 (128K+ 레코드)
├── 승하차.csv              # ✅ 승하차 데이터
├── node_features.csv       # ✅ 역별 노드 특징 (22개 역 × 16차원)
├── date_features.csv       # ✅ 날짜별 특징 (243일 × 21차원)
├── time_features.csv       # ✅ 시간대별 특징 (24시간 × 7차원)
├── adjacency_matrix.npy    # ✅ 그래프 인접행렬 (22×22)
└── seasonal_split.pkl      # ✅ 데이터 분할 정보 (Train/Val/Test)
```

### 2. **선택적 데이터** (이미 전처리된 경우)
```
data/
├── train_dataset.pkl       # ⚠️ 선택적 (없으면 자동 생성)
├── val_dataset.pkl         # ⚠️ 선택적 (없으면 자동 생성)
└── test_dataset.pkl        # ⚠️ 선택적 (없으면 자동 생성)
```

---

## 🎯 학습 실행 방법

### **1. 처음부터 학습**
```bash
python scripts/train_occupancy_model.py --config config/model_config.yaml
```

### **2. 체크포인트에서 이어서 학습**
```bash
python scripts/train_occupancy_model.py \
    --config config/model_config.yaml \
    --resume checkpoints/occupancy_model/checkpoint_epoch_25.pth
```

### **3. 백그라운드 학습**
```bash
nohup python scripts/train_occupancy_model.py \
    --config config/model_config.yaml \
    --resume checkpoints/occupancy_model/checkpoint_epoch_25.pth \
    > logs/training_$(date +%Y%m%d_%H%M%S).log 2>&1 &
```

---

## 📦 의존성 패키지

### **필수 패키지**
```python
torch              # PyTorch
numpy              # 수치 연산
pandas             # 데이터 처리
yaml               # 설정 파일 파싱
geopy              # 지리적 거리 계산 (그래프 생성용)
```

### **설치 방법**
```bash
pip install torch numpy pandas pyyaml geopy
```

---

## 🔍 파일별 역할

### **1. `models/dcrnn_layers.py`**
- `DiffusionConvolution`: 그래프 확산 합성곱
- `DCGRUCell`: Diffusion Convolutional GRU 셀
- `AttentionMechanism`: Attention 메커니즘
- `DCRNN`: 전체 모델 (Encoder-Decoder + Attention)

### **2. `models/dataset.py`**
- `SubwayGraphDataset`: PyTorch Dataset 클래스
- 시계열 데이터 슬라이딩 윈도우 생성
- 배치 텐서 구성

### **3. `scripts/train_occupancy_model.py`**
- `OccupancyDataset`: 혼잡도 전용 데이터셋 (Log1p + 정규화)
- `OccupancyTrainer`: 학습 클래스
  - 데이터 로드
  - 모델 구축
  - 학습 루프
  - 체크포인트 저장

### **4. `config/model_config.yaml`**
- 모델 아키텍처 설정
- 학습 하이퍼파라미터
- 데이터 설정
- 옵티마이저/스케줄러 설정

---

## ✅ 최소 필수 파일 체크리스트

### **코드 파일 (5개)**
- [x] `models/dcrnn_layers.py`
- [x] `models/dataset.py`
- [x] `models/losses.py`
- [x] `scripts/train_occupancy_model.py`
- [x] `config/model_config.yaml`

### **데이터 파일 (7개)**
- [x] `data/혼잡도.csv`
- [x] `data/승하차.csv`
- [x] `data/node_features.csv`
- [x] `data/date_features.csv`
- [x] `data/time_features.csv`
- [x] `data/adjacency_matrix.npy`
- [x] `data/seasonal_split.pkl`

---

## 🚀 빠른 시작 가이드

### **1. 환경 확인**
```bash
# Python 버전 확인
python --version  # 3.8 이상 권장

# 필수 패키지 확인
python -c "import torch, numpy, pandas, yaml; print('✅ All packages installed')"
```

### **2. 데이터 확인**
```bash
# 핵심 데이터 파일 존재 확인
ls -lh data/{혼잡도.csv,승하차.csv,node_features.csv,date_features.csv,time_features.csv,adjacency_matrix.npy}
```

### **3. 학습 시작**
```bash
# 처음부터 학습
python scripts/train_occupancy_model.py --config config/model_config.yaml

# 또는 체크포인트에서 이어서
python scripts/train_occupancy_model.py \
    --config config/model_config.yaml \
    --resume checkpoints/occupancy_model/checkpoint_epoch_25.pth
```

---

## 📝 참고사항

1. **데이터셋 자동 생성**: `train_dataset.pkl` 등이 없어도 자동으로 생성됩니다.
2. **체크포인트 저장**: 매 epoch마다 `checkpoints/occupancy_model/checkpoint_epoch_N.pth`에 저장됩니다.
3. **Best 모델**: Validation loss가 개선될 때마다 `best_occupancy_model.pth`에 저장됩니다.
4. **로그 파일**: 학습 로그는 `logs/` 디렉토리에 저장됩니다.

---

**💡 핵심**: 위 12개 파일만 있으면 학습을 시작할 수 있습니다!

